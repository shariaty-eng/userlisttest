import { Size } from "@/types/size.type"
import { Variant } from "@/types/variant.type"

export type AvatarProps={
    src?:string, 
    alt?:string, 
    className?:string, 
    variant?:Variant, 
    size?:Size,
}